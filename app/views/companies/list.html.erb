<%= form_for :company, :html => { :method => :edit } do |f| %>
  <%= f.select :companies_id, options_for_select(@companies.collect { |c| [ c.nome, c.id ] }, 1), {:prompt=>"Selecione a empresa"}, { :class => 'form-control', :required => true, id: 'companies_id' } %>
  <% end %>